package demos

model Funnel {
  
  random RealVar h ?: latentReal
  
  param int N ?: 1
  
  random List<RealVar> x ?: latentRealList(N)
  
  laws {
    
    h ~ Normal(0, 1)
    
    //x ?: Normal(0, 1)
    
    
    
  }
}